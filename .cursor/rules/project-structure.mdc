---
description: ChatCMS 工程目录与模块职责；改结构时必须同步更新本文件
alwaysApply: true
---

# ChatCMS 工程结构

栈：**React 19 + Vite + Zustand** 前端 · **Rust + Tauri v2** 后端。

## 维护约定（强制）

- 本文件是**当前目录真相**；新增/移动/删除模块或改职责后，**同一任务内**更新本 rule。
- 更细的「如何扫树、怎么改」见 skill：`.cursor/skills/update-project-structure/SKILL.md`。

## 顶层

```text
chatcms/
├── src/                 # React 前端
├── src-tauri/           # Tauri / Rust 后端
├── scripts/             # 工程脚本与静态资源
│   ├── dev.sh           # 开发启动脚本
│   ├── database/        # 数据库脚本
│   │   └── schema.sql   # SQLite 建表 DDL（db.rs 编译期 include_str! 加载）
│   └── skills/          # 内置 Skill .md（seed.rs 编译期 include_str! 加载）
│       ├── content-publish.md
│       ├── image-brief.md
│       └── skill-creator.md
├── bin/                 # 打包脚本（mac / windows）
├── docs/                # VitePress 文档站（pnpm docs:dev / docs:build）
│   ├── .vitepress/      # 站点配置
│   ├── guide/           # 概览 / 启动
│   ├── concepts/        # Agent Loop、多 Agent
│   ├── features/        # 功能说明
│   ├── drawio/          # 分层流程图源（.drawio）
│   └── public/          # 静态资源
├── resume/              # 简历用项目经历文稿（非运行时）
├── .github/workflows/   # CI（含 docs.yml → GitHub Pages）
├── .cursor/rules/       # Cursor rules（含本文件）
├── .cursor/skills/      # 项目 skills
├── package.json         # 前端 + 文档站依赖；pnpm tauri / docs:* / package*
└── prompt.md            # 开发进度备忘（非运行时）
```

忽略：`node_modules/`、`src-tauri/target/`、`dist/`、`release/`、`docs/.vitepress/dist`。

## 前端 `src/`

**规范**：`.cursor/rules/chatcms-ui.mdc`（目录、ES6、箭头函数）；主列表表格见 `.cursor/rules/cabinx-table.mdc`。

```text
src/
├── App.tsx / main.tsx      # HashRouter + 一级路由
├── layout/
│   ├── AppLayout.tsx       # Sidebar + Topbar + Outlet
│   ├── nav.ts              # 导航：工作台 / 媒体管理 / 智能配置 / 系统设置分组
│   └── components/         # Sidebar、Topbar、ThemeToggle、PageShell
├── views/
│   ├── chat/               # 智能会话（助手回复：一键保存 / AI整理；可回跳）
│   ├── map/                # 业务地图看板（分区入口 / 笔记 / 时钟）
│   ├── content/            # 内容管理 · AI 笔记（SQLite cms_note；Novel 富文本；桥接 knowledge 供 Agent 检索）
│   ├── images/             # 图片工厂 · AI 生图与本地保存
│   ├── videos/             # 视频工厂 · AI 生视频 / MPT 口播工程页 / 本地保存
│   ├── schedules/          # 调度中心 · 项目列表 + n8n 风格工作流画布
│   ├── skills/             # 技能管理 · OpenClaw SKILL.md CRUD
│   ├── agents/             # 代理管理 · 多 Agent 档案 / 技能白名单
│   ├── mcp/                # MCP 管理 · 表格/预设/工具列表
│   ├── models/             # 模型配置表格 + 弹窗 CRUD
│   ├── intents/            # 意图规则 · 关键词/权重/启用（软路由配置）
│   ├── permissions/        # 权限模式面板组件（路由在 settings）
│   ├── settings/           # 全局配置 / 权限 / 意图评测 / 运行观测（审计+会话链路）/ 渠道；知识库页文件亦在此（导航在智能配置）
│   ├── media-platforms/    # 插件脚本（导航在媒体管理）· 发布/采集注入页
│   ├── crawler/            # 媒体采集（列表 + /crawler/:id 配置页 · chatcms-collect）
│   ├── accounts/           # 账号管理（媒体管理 · 本地密码本）
│   ├── nav/                # 导航书签
│   └── publish/            # 发布到浏览器插件（自动填表桥接弹窗）
├── components/
│   ├── CabinX/             # 通用业务组件库
│   ├── Title/              # 页面标题组件
│   ├── Select.tsx
│   ├── Soon.tsx
│   └── icons/
├── hooks/
│   └── useTauri.ts
├── styles/
│   ├── tokens.css
│   ├── global.css
│   └── sty.ts              # 样式工具函数
├── stores/
│   ├── useChatStore.ts
│   ├── useProviderStore.ts
│   ├── usePermissionStore.ts # 权限模式列表 + 当前激活
│   ├── useNotesStore.ts    # 内容管理笔记（SQLite via notes_*；可写知识库桥接）
│   └── useThemeStore.ts    # 外观：system / light / dark
├── config/
│   ├── modelPresets.ts     # 模型预设（Claude/GPT/DeepSeek/Kimi/千问）
│   ├── platformPresets.ts  # 内容平台预设（微信/抖音/小红书等）
│   ├── mcpPresets.ts       # 常用 MCP 服务器预设
│   ├── channelPresets.ts   # 聊天渠道目录（Telegram/Discord/飞书等）
│   └── publishPlatforms.ts # 浏览器发布平台（对齐 chatcms-extesion）
└── types/
    └── index.ts
```

## 后端 `src-tauri/src/`

```text
src-tauri/src/
├── main.rs              # 二进制入口 → lib::run
├── lib.rs               # Tauri commands 注册 + bootstrap（setup）
├── db.rs                # SQLite 连接池；路径走 common::config::resolve_data_root；schema include_str!
├── common/              # 跨域基础能力
│   ├── mod.rs           # logging / config / persist / permission / provider
│   ├── logging/         # 应用日志（tauri-plugin-log + AppEnv）
│   ├── config/          # AppConfig、数据根路径、全局配置命令
│   ├── persist/         # 尚无独立 repository 的域的 store JSON 暂存
│   ├── permission/      # 权限内核：模式、裁决、会话授权、审计
│   └── provider/        # 模型流式请求与一次性补全
│
├── mcp/                 # MCP stdio 子进程管理
│   ├── mod.rs
│   ├── manager.rs       # McpManager：connect / disconnect / reconnect
│   ├── client.rs        # stdio 子进程 JSON-RPC 客户端
│   ├── config.rs        # McpServerConfig
│   ├── types.rs
│   └── commands.rs      # mcp_* Tauri commands
├── scripts/             # OpenClaw 风格 Skills（SKILL.md）
│   ├── mod.rs           # 再导出：Skill / ensure_seeded / list / format_for_prompt
│   ├── types.rs         # Skill / SkillSource
│   ├── seed.rs          # 首次启动写出内置 .md 并入库；include_str! 从 scripts/skills/
│   ├── store.rs         # CRUD + npx 安装（调 persist 层）
│   ├── prompt.rs        # format_for_prompt / to_skill_md
│   └── commands.rs      # skill_* Tauri commands
│
├── chat/                # 智能会话数据与命令（运行分层在 core/）
│   ├── mod.rs           # Session / SessionGroup / Message / Role
│   ├── repository.rs    # SQLite sessions + messages + session_group 读写
│   ├── service.rs       # 会话 CRUD（ensure / list / 分组）
│   ├── commands.rs      # chat_send + chat_complete + session_* + session_group_* + chat_trace_list
│   └── trace.rs         # 会话链路观测（分段耗时 + 落库）
├── core/                # Agent 运行分层
│   ├── mod.rs           # 门面：分层声明 + start_turn
│   ├── turn.rs          # 总编排（Dispatcher）：按层调用 run
│   ├── context.rs       # TurnContext（一轮对话上下文）
│   ├── pipeline.rs      # 层间钩子（日志 / trace）
│   ├── session/         # 会话层 run
│   ├── intent/          # 意图层 run / infer
│   ├── decision/        # 决策层 run：模式、角色、工具集合
│   ├── plan/            # 编排层 run：prompt + Agent Loop
│   ├── execute/         # 执行层 run：工具回合（委托 agents::dispatch）
│   ├── memory/          # 记忆协作：知识注入、截断、快照
│   ├── response/        # 响应层 run：助手终态 + 链路落库
│   └── tools/           # 内置工具（bash / read_file / write_file / spawn_agent / …）
├── accounts/            # 多平台账号 CRUD + 主密码保险柜
│   ├── mod.rs
│   ├── vault.rs         # Argon2id + AES-256-GCM
│   └── commands.rs      # account_* / vault_* commands
├── agents/              # 多代理档案与权限分流
│   ├── mod.rs           # AgentProfile / bundled_agents；再导出运行分层
│   ├── repository.rs    # SQLite agent 表读写 + AppConfig 委托
│   ├── service.rs       # ensure_seeded / list / add / update / activate / remove
│   ├── commands.rs      # agent_* Tauri commands
│   ├── state.rs         # AgentState（sessions / skills / agents / grants）
│   ├── dispatch.rs      # 工具路由前 authorize（spawn / MCP / 内置）
│   ├── subagent.rs      # 子 agent loop
├── bmarks/              # 导航书签 CRUD
│   ├── mod.rs
│   └── commands.rs
├── bridge/              # 浏览器插件桥接（127.0.0.1:17890）
│   └── mod.rs
├── business_map/        # 业务地图状态持久化
│   ├── mod.rs
│   └── commands.rs
├── channels/            # IM 渠道运行时（Telegram 等）
│   ├── mod.rs
│   └── commands.rs
├── crawler/             # 采集器（chatcms-collect worker）HTTP 客户端 + 任务 CRUD；base_url 与 AppConfig.general 同步
│   ├── mod.rs
│   ├── repository.rs
│   ├── service.rs
│   └── commands.rs
├── images/              # AI 生图 + 本地落盘（目录走 resolve_data_root）
│   ├── mod.rs
│   └── commands.rs
├── kbase/               # 知识库（笔记 / 公开 feed / 检索）
│   ├── mod.rs
│   ├── types.rs
│   ├── search.rs
│   ├── feed.rs
│   ├── export.rs
│   └── commands.rs
├── medias/              # 媒体平台 + 稿件（发布 / 采集脚本 · SQLite）
│   ├── mod.rs
│   ├── repository.rs    # media_platform / media_publish_script / media_collect_script
│   ├── service.rs
│   └── commands.rs
├── notes/               # 内容管理笔记（cms_note / cms_note_group；桥接 knowledge）
│   ├── mod.rs
│   ├── repository.rs
│   ├── service.rs
│   └── commands.rs
├── mpt/                 # chatcms-video 客户端；base_url 与 AppConfig.general 同步
│   ├── mod.rs
│   └── commands.rs      # mpt_* Tauri commands
├── publish/             # 本机发布桥 axum server（端口读 AppConfig.general.publish_bridge_port）
│   ├── mod.rs
│   └── commands.rs
├── schedules/           # 调度中心项目 + 工作流图 CRUD
│   ├── mod.rs
│   └── commands.rs
└── videos/              # AI 生视频（Videos API）+ 本地落盘（目录走 resolve_data_root）
    ├── mod.rs
    └── commands.rs
```

## 关键数据流

```text
ChatWindow → invoke chat_send { session_id?, content, mode?, group_id? }
 → chat::commands::chat_send
 → core::start_turn
 → session::run
 → intent::run
 → decision::run（会话已绑定 agent 则沿用；Agent 模式可按意图选角并绑定）
 → plan::run → plan::run_agent_loop
 → common::provider::stream_chat（SSE）
 → execute::run → agents::dispatch
 → response::finalize_assistant_turn
 → response::run（链路落库）
 → memory（知识注入 / 工具结果截断 / 会话快照）

workspace 绑定规则：
 每个 Agent 有且仅有一个 workspace（~/…/workspaces/<slug>/）
 新建会话时 workspace 暂取全局激活 Agent；decision 绑定角色时可写入 Session.agent_id / workspace_dir
 整个会话生命周期内路径不变，切换 Agent 不影响历史会话的 workspace
```

工具分流见 `agents/dispatch.rs`；原理文档：`docs/concepts/agent-loop.md`、`docs/concepts/multi-agent.md`。

## 改结构时怎么动本文件

| 变更 | 更新位置 |
|------|----------|
| 新增/删除 `src/components/*` | 「前端 `src/`」树 |
| 新增/拆分 `src-tauri/src/**` | 「后端」树 + 必要时数据流 |
| 新增/移动 `scripts/**` | 「顶层」树 + 相关 `include_str!` 路径说明 |
| 改 Tauri command 入口职责 | `lib.rs` 说明或数据流 |
| 仅改函数内部逻辑、不改路径 | **不必**改本 rule |
