---
description: ChatCMS 工程目录与模块职责；改结构时必须同步更新本文件
alwaysApply: true
---

# ChatCMS 工程结构

栈：**React 19 + Vite + Zustand** 前端 · **Rust + Tauri v2** 后端。

## 维护约定（强制）

- 本文件是**当前目录真相**；新增/移动/删除模块或改职责后，**同一任务内**更新本 rule。
- 更细的「如何扫树、怎么改」见 skill：`.cursor/skills/update-project-structure/SKILL.md`。

## 顶层

```text
chatcms/
├── src/                 # React 前端
├── src-tauri/           # Tauri / Rust 后端
├── scripts/             # 工程脚本与静态资源
│   ├── dev.sh           # 开发启动脚本
│   ├── database/        # 数据库脚本
│   │   └── schema.sql   # SQLite 建表 DDL（db.rs 编译期 include_str! 加载）
│   └── skills/          # 内置 Skill .md（seed.rs 编译期 include_str! 加载）
│       ├── content-publish.md
│       ├── image-brief.md
│       └── skill-creator.md
├── bin/                 # 打包脚本（mac / windows）
├── doc/                 # 原理说明（如 how-it-works.md）
├── .cursor/rules/       # Cursor rules（含本文件）
├── .cursor/skills/      # 项目 skills
├── package.json         # 前端依赖；脚本 pnpm tauri / pnpm package*
└── prompt.md            # 开发进度备忘（非运行时）
```

忽略：`node_modules/`、`src-tauri/target/`、`dist/`、`release/`。

## 前端 `src/`

**规范**：`.cursor/rules/chatcms-ui.mdc`（目录、ES6、箭头函数）。

```text
src/
├── App.tsx / main.tsx      # HashRouter + 一级路由
├── layout/
│   ├── AppLayout.tsx       # Sidebar + Topbar + Outlet
│   ├── nav.ts              # 导航：干活区 + 智能配置 / 系统设置二级
│   └── components/         # Sidebar、Topbar、ThemeToggle
├── views/
│   ├── chat/               # 智能会话（SessionList + ChatWindow + PermissionPrompt）
│   ├── map/                # 业务地图看板（分区入口 / 笔记 / 时钟）
│   ├── content/            # 内容管理 · AI 笔记（Notion 风格）
│   ├── images/             # 图片管理 · AI 生图与本地保存
│   ├── videos/             # 视频管理 · AI 生视频 / MPT 口播工程页 / 本地保存
│   ├── accounts/           # 账号管理 · 本地密码本（序号/跳转/主密码解锁查看）
│   ├── schedules/          # 任务调度 · 项目列表 + n8n 风格工作流画布
│   ├── skills/             # 技能管理 · OpenClaw SKILL.md CRUD
│   ├── agents/             # 代理管理 · 多 Agent 档案 / 技能白名单
│   ├── mcp/                # MCP 管理 · 表格/预设/工具列表
│   ├── permissions/        # 权限模式面板组件（路由在 settings）
│   ├── model-config/       # 模型配置表格 + 弹窗 CRUD
│   ├── settings/           # 系统设置页：权限 / 记忆 / 渠道
│   ├── media-platforms/    # 媒体平台管理
│   ├── crawler/            # 采集器管理
│   ├── nav/                # 导航书签
│   └── publish/            # 发布到浏览器插件（自动填表桥接弹窗）
├── components/
│   ├── CabinX/             # 通用业务组件库
│   ├── Title/              # 页面标题组件
│   ├── Select.tsx
│   ├── Soon.tsx
│   └── icons/
├── hooks/
│   └── useTauri.ts
├── styles/
│   ├── tokens.css
│   ├── global.css
│   └── sty.ts              # 样式工具函数
├── stores/
│   ├── useChatStore.ts
│   ├── useProviderStore.ts
│   ├── usePermissionStore.ts # 权限模式列表 + 当前激活
│   └── useThemeStore.ts    # 外观：system / light / dark
├── config/
│   ├── modelPresets.ts     # 模型预设（Claude/GPT/DeepSeek/Kimi/千问）
│   ├── platformPresets.ts  # 内容平台预设（微信/抖音/小红书等）
│   ├── mcpPresets.ts       # 常用 MCP 服务器预设
│   ├── channelPresets.ts   # 聊天渠道目录（Telegram/Discord/飞书等）
│   └── publishPlatforms.ts # 浏览器发布平台（对齐 chatcms-extesion）
└── types/
    └── index.ts
```

## 后端 `src-tauri/src/`

```text
src-tauri/src/
├── main.rs              # 二进制入口 → lib::run
├── lib.rs               # Tauri commands 注册 + bootstrap（setup）
├── db.rs                # SQLite 连接池；include_str! 加载 scripts/database/schema.sql
│
├── agent/               # Agent 运行时（单一职责拆分）
│   ├── mod.rs           # 再导出：AgentState / send_message / commands / …
│   ├── core/            # 核心会话循环
│   │   ├── mod.rs       # 再导出 core 内各项
│   │   ├── state.rs     # AgentState（含 session_grants / skills / agents）
│   │   ├── chat.rs      # 主会话 + agent loop，组装 system prompt
│   │   ├── dispatch.rs  # 工具路由前 authorize（spawn / MCP / 内置）
│   │   ├── subagent.rs  # 子 agent loop
│   │   ├── permission.rs# 权限请求弹窗与 resolve（含 remember）
│   │   ├── turns.rs     # turn 组装、tool 事件 emit
│   │   ├── commands.rs  # chat_send / session_* Tauri commands
│   │   └── agents/      # 多代理档案
│   │       ├── mod.rs   # Agent 类型 / 默认代理 / ensure_seeded
│   │       └── commands.rs # agent_* Tauri commands
│   ├── mcp/             # MCP stdio 子进程管理
│   │   ├── mod.rs
│   │   ├── manager.rs   # McpManager：connect / disconnect / reconnect
│   │   ├── client.rs    # stdio 子进程 JSON-RPC 客户端
│   │   ├── config.rs    # McpServerConfig
│   │   ├── types.rs
│   │   └── commands.rs  # mcp_* Tauri commands
│   ├── memory/          # Session / Message 读写（SQLite via persist）
│   │   └── mod.rs
│   ├── permission/      # 权限内核
│   │   ├── mod.rs       # 再导出
│   │   ├── types.rs     # PermissionMode / DomainPolicy / Decision
│   │   ├── config.rs    # PermissionConfig CRUD
│   │   ├── authz.rs     # 决策引擎（evaluate_request）
│   │   ├── grants.rs    # 会话级临时授权（session_grants）
│   │   ├── audit.rs     # 审计日志写入
│   │   └── commands.rs  # permission_* Tauri commands
│   ├── provider/        # AI Provider 流式请求
│   │   ├── mod.rs       # stream_chat 分发
│   │   ├── anthropic.rs # Claude API（SSE + tools）
│   │   ├── openai.rs    # OpenAI 兼容（SSE + tools）
│   │   └── types.rs     # ProviderConfig / StreamChunk
│   ├── scripts/         # OpenClaw 风格 Skills（SKILL.md）
│   │   ├── mod.rs       # 再导出：Skill / ensure_seeded / list / format_for_prompt
│   │   ├── types.rs     # Skill / SkillSource
│   │   ├── seed.rs      # 首次启动写出内置 .md 并入库；include_str! 从 scripts/skills/
│   │   ├── store.rs     # CRUD + npx 安装（调 persist 层）
│   │   ├── prompt.rs    # format_for_prompt / to_skill_md
│   │   └── commands.rs  # skill_* Tauri commands
│   └── tools/           # 内置工具实现
│       ├── mod.rs       # ToolDef / execute 分发
│       ├── types.rs
│       ├── bash.rs
│       ├── read_file.rs
│       ├── write_file.rs
│       └── spawn_agent.rs
│
├── accounts/            # 多平台账号 CRUD + 主密码保险柜
│   ├── mod.rs
│   ├── vault.rs         # Argon2id + AES-256-GCM
│   └── commands.rs      # account_* / vault_* commands
├── agents/              # 代理档案（旧路径别名，实体在 agent/core/agents）
│   └── commands.rs
├── bmarks/              # 导航书签 CRUD
│   ├── mod.rs
│   └── commands.rs
├── bridge/              # 浏览器插件桥接（127.0.0.1:17890）
│   └── mod.rs
├── business_map/        # 业务地图状态持久化
│   ├── mod.rs
│   └── commands.rs
├── channels/            # IM 渠道运行时（Telegram 等）
│   ├── mod.rs
│   └── commands.rs
├── config/              # AppConfig / Provider CRUD
│   ├── mod.rs
│   └── commands.rs
├── crawler/             # 采集器（chatcms-collect worker）HTTP 客户端
│   ├── mod.rs
│   └── commands.rs
├── images/              # AI 生图 + 本地落盘
│   ├── mod.rs
│   └── commands.rs
├── kbase/               # 知识库（笔记 / 公开 feed / 检索）
│   ├── mod.rs
│   ├── types.rs
│   ├── search.rs
│   ├── feed.rs
│   ├── export.rs
│   └── commands.rs
├── medias/              # 媒体平台 + 稿件（发布 / 采集脚本）
│   ├── mod.rs
│   └── commands.rs
├── mpt/                 # MoneyPrinterTurbo 客户端
│   ├── mod.rs
│   └── commands.rs      # mpt_* Tauri commands
├── persist/             # 持久化层（SQLite + tauri-plugin-store）
│   ├── mod.rs           # 统一导出所有 save_* / load_* / delete_*
│   ├── sessions.rs      # SQLite
│   ├── knowledge.rs     # SQLite
│   ├── permission.rs    # SQLite（审计日志）
│   ├── skills.rs        # SQLite（skill_pkg 表）
│   ├── images.rs        # SQLite
│   ├── videos.rs        # SQLite
│   ├── config.rs        # store JSON
│   ├── mcp.rs           # store JSON
│   ├── channels.rs      # store JSON
│   ├── mpt.rs           # store JSON
│   ├── accounts.rs      # store JSON（加密）
│   ├── media_platforms.rs # store JSON
│   ├── nav.rs           # store JSON
│   ├── schedules.rs     # store JSON
│   ├── agents.rs        # store JSON
│   ├── business_map.rs  # store JSON
│   ├── bridge.rs        # store JSON
│   └── crawler.rs       # store JSON
├── publish/             # 本机发布桥 axum server
│   ├── mod.rs
│   └── commands.rs
├── schedules/           # 任务调度项目 + 工作流图 CRUD
│   ├── mod.rs
│   └── commands.rs
└── videos/              # AI 生视频（Videos API）+ 本地落盘
    ├── mod.rs
    └── commands.rs
```

## 关键数据流

```text
ChatWindow → 权限下拉 set_active → PermissionConfig.active_mode_id
ChatWindow → invoke chat_send
  → agent::core::send_message (chat.rs)
  → provider::stream_chat (+ tools schema)
  → dispatch authorize（当前模式 domains）→ ask → emit permission-request
  → emit stream-chunk / tool-call / tool-result
  → store/chat.ts → UI
```

工具分流见 `agent/core/dispatch.rs`；原理文档：`doc/how-it-works.md`。

## 改结构时怎么动本文件

| 变更 | 更新位置 |
|------|----------|
| 新增/删除 `src/components/*` | 「前端 `src/`」树 |
| 新增/拆分 `src-tauri/src/**` | 「后端」树 + 必要时数据流 |
| 新增/移动 `scripts/**` | 「顶层」树 + 相关 `include_str!` 路径说明 |
| 改 Tauri command 入口职责 | `lib.rs` 说明或数据流 |
| 仅改函数内部逻辑、不改路径 | **不必**改本 rule |
