---
description: ChatCMS 工程目录与模块职责；改结构时必须同步更新本文件
alwaysApply: true
---

# ChatCMS 工程结构

栈：**React 19 + Vite + Zustand** 前端 · **Rust + Tauri v2** 后端。

## 维护约定（强制）

- 本文件是**当前目录真相**；新增/移动/删除模块或改职责后，**同一任务内**更新本 rule。
- 更细的「如何扫树、怎么改」见 skill：`.cursor/skills/update-project-structure/SKILL.md`。

## 顶层

```text
chatcms/
├── src/                 # React 前端
├── src-tauri/           # Tauri / Rust 后端
├── doc/                 # 原理说明（如 how-it-works.md）
├── .cursor/rules/       # Cursor rules（含本文件）
├── .cursor/skills/      # 项目 skills
├── package.json         # 前端依赖；脚本 pnpm tauri / pnpm dev
└── prompt.md            # 开发进度备忘（非运行时）
```

忽略：`node_modules/`、`src-tauri/target/`、`dist/`。

## 前端 `src/`

**规范**：`.cursor/rules/chatcms-ui.mdc`（目录、ES6、箭头函数）。

```text
src/
├── App.tsx / main.tsx      # HashRouter + 一级路由
├── layout/
│   ├── AppLayout.tsx       # Sidebar + Topbar + Outlet
│   ├── nav.ts              # 一级导航项与标题
│   └── components/         # Sidebar、Topbar、Settings/Permission 弹窗
├── views/
│   ├── chat/               # 智能会话（SessionList + ChatWindow）
│   ├── map/                # 业务地图看板（分区入口 / 笔记 / 时钟）
│   ├── content/            # 内容管理 · AI 笔记（Notion 风格）
│   ├── images/             # 图片管理 · AI 生图与本地保存
│   ├── videos/             # 视频管理 · AI 生视频与本地保存
│   ├── accounts/           # 账号管理 · 多平台账号与密钥 CRUD
│   ├── schedules/          # 任务调度 · 项目列表 + n8n 风格工作流画布
│   ├── skills/             # 技能管理 · OpenClaw SKILL.md CRUD
│   ├── agents/             # 代理管理 · 多 Agent 档案 / 技能白名单
│   ├── mcp/                # MCP 管理 · 表格/预设/工具列表
│   ├── model-config/       # 模型配置表格 + 弹窗 CRUD
│   └── settings/components/ # SettingsModal 用 Knowledge/Channel 面板
├── components/
│   ├── PlaceholderPage.tsx
│   └── icons/
├── hooks/
│   └── useTauri.ts
├── styles/
│   ├── tokens.css
│   └── global.css
├── stores/
│   ├── useChatStore.ts
│   └── useProviderStore.ts
├── config/
│   ├── modelPresets.ts     # 模型预设（Claude/GPT/DeepSeek/Kimi/千问）
│   ├── platformPresets.ts  # 内容平台预设（微信/抖音/小红书等）
│   └── mcpPresets.ts       # 常用 MCP 服务器预设
└── types/
    └── index.ts
```

## 后端 `src-tauri/src/`

```text
src-tauri/src/
├── main.rs              # 二进制入口 → lib::run
├── lib.rs               # Tauri commands 注册 + bootstrap
├── agent/               # Agent 运行时（单一职责拆分）
│   ├── mod.rs           # 再导出：AgentState / send_message / …
│   ├── state.rs         # AgentState
│   ├── chat.rs          # 主会话 + agent loop
│   ├── dispatch.rs      # 工具路由（spawn / MCP / 内置）
│   ├── subagent.rs      # 子 agent loop
│   ├── permission.rs    # 权限请求与 resolve
│   └── turns.rs         # turn 组装、tool 事件 emit
├── provider/mod.rs      # Anthropic / OpenAI 流式 + tools 请求体
├── tools/mod.rs         # ToolDef / execute（read_file、write_file、bash、spawn_agent）
├── mcp/mod.rs           # MCP stdio 子进程
├── memory/mod.rs        # Session / Message
├── knowledge/mod.rs     # 知识库检索 → system prompt
├── channels/mod.rs      # Telegram poller
├── accounts/mod.rs      # 多平台账号与密钥 CRUD
├── schedules/mod.rs     # 任务调度项目 + 工作流图 CRUD
├── skills/mod.rs        # OpenClaw 风格 Skills（SKILL.md）+ 提示词注入
├── agents/mod.rs        # 多代理档案 / 默认代理 / 技能白名单
├── images/mod.rs        # AI 生图 + 本地落盘
├── videos/mod.rs        # AI 生视频（Videos API）+ 本地落盘
├── config/mod.rs        # AppConfig / ProviderConfig / ProviderProfile CRUD
└── persist/mod.rs       # tauri-plugin-store 持久化
```

## 关键数据流

```text
ChatWindow → invoke chat_send
  → agent::send_message (chat.rs)
  → provider::stream_chat (+ tools schema)
  → emit stream-chunk / tool-call / tool-result / permission-request
  → store/chat.ts → UI
```

工具分流见 `agent/dispatch.rs`；原理文档：`doc/how-it-works.md`。

## 改结构时怎么动本文件

| 变更 | 更新位置 |
|------|----------|
| 新增/删除 `src/components/*` | 「前端 `src/`」树 |
| 新增/拆分 `src-tauri/src/**` | 「后端」树 + 必要时数据流 |
| 改 Tauri command 入口职责 | `lib.rs` 说明或数据流 |
| 仅改函数内部逻辑、不改路径 | **不必**改本 rule |
