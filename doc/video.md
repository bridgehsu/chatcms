# 视频服务

制片页同一入口，成片进同一视频库（数据目录 `videos/`）。

- **口播剪辑**：本机 chatcms-video，默认 `http://127.0.0.1:6060`。按文案检索素材，配音、字幕后拼接。
- **模型生成**：当前 OpenAI 兼容供应商的 `/v1/videos`（`sora-2` / `sora-2-pro`）。`9:16` 为 `720×1280`，其余 `1280×720`；时长取 4 / 8 / 12 秒。Anthropic 不可用。

共用主题、文案、画幅、时长。可灵、Runway 等三方模型尚未接入。
