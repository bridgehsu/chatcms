# 工程文稿（非站点）

本目录存放**设计笔记与流程图源文件**（如 `agent-loop.md`、`drawio/`）。

对外文档站在仓库根目录的 [`docs/`](../docs/)（VitePress），本地预览：

```bash
pnpm docs:dev
```

推送 `main` 后由 GitHub Actions 部署到 GitHub Pages。内容有更新时，请同步改 `docs/` 下对应页面。
